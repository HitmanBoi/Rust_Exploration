fn main() {
    //Conditionals 
    //Condition is just any expresssion that evaluates to true or false or the Boolean data type

    // < less than
    let cond = 1 < 2 ;
    println!("1 < 2 is {}",cond);

    // > greater than
    let cond = 1 > 2;
    println!("1 > 2 is {}",cond);

}
